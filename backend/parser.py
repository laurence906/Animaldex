# helper functions to parse speciesnet output

# getClasses(dict imageOutput) returns the classes identified alongside the confidence score
# getPredictionScore(dict imageOutput) returns overall prediction score assigned by model